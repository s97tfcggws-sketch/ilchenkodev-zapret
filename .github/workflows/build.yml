name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

# ─────────────────────────────────────────────────────────────────────────────
# macOS — builds a .app (hidden from Dock via LSUIElement=True) → .dmg
# ─────────────────────────────────────────────────────────────────────────────
jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          # pyobjc is the umbrella package — installs Cocoa, Foundation, AppKit, etc.
          pip install pyinstaller pillow pystray pyobjc

      - name: Build .app with PyInstaller
        working-directory: dist_mac
        run: |
          pyinstaller --noconfirm --clean ILCHENKODEV.spec

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Create .dmg
        run: |
          mkdir -p release
          # create-dmg with nice installer window; fallback to hdiutil if it fails
          create-dmg \
            --volname "ilchenkodev-zapret" \
            --window-pos 200 120 \
            --window-size 540 380 \
            --icon-size 128 \
            --icon "ilchenkodev-zapret.app" 130 190 \
            --hide-extension "ilchenkodev-zapret.app" \
            --app-drop-link 410 190 \
            --no-internet-enable \
            "release/ilchenkodev-zapret-for-mac.dmg" \
            "dist_mac/dist/ilchenkodev-zapret.app" \
          || hdiutil create \
               -volname "ilchenkodev-zapret" \
               -srcfolder "dist_mac/dist/ilchenkodev-zapret.app" \
               -ov -format UDZO \
               "release/ilchenkodev-zapret-for-mac.dmg"

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: ilchenkodev-zapret-macOS-DMG
          path: release/ilchenkodev-zapret-for-mac.dmg

# ─────────────────────────────────────────────────────────────────────────────
# Windows — single standalone .exe, tray only, no console, no taskbar window
# ─────────────────────────────────────────────────────────────────────────────
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pillow pystray

      - name: Build .exe with PyInstaller
        working-directory: dist_win
        shell: pwsh
        run: |
          pyinstaller --noconfirm --clean ILCHENKODEV.spec

      - name: Collect release files
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path release
          Copy-Item "dist_win\dist\ilchenkodev-zapret.exe" "release\ilchenkodev-zapret-for-win.exe"

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: ilchenkodev-zapret-Windows-EXE
          path: release/ilchenkodev-zapret-for-win.exe

# ─────────────────────────────────────────────────────────────────────────────
# Release — attaches artifacts to GitHub Release when a version tag is pushed
# ─────────────────────────────────────────────────────────────────────────────
  release:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - name: Download macOS DMG
        uses: actions/download-artifact@v4
        with:
          name: ilchenkodev-zapret-macOS-DMG
          path: artifacts

      - name: Download Windows EXE
        uses: actions/download-artifact@v4
        with:
          name: ilchenkodev-zapret-Windows-EXE
          path: artifacts

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "ilchenkodev-zapret ${{ github.ref_name }}"
          body: |
            ## ilchenkodev-zapret ${{ github.ref_name }}

            ### Скачать
            | Платформа | Файл |
            |-----------|------|
            | macOS | `ilchenkodev-zapret-for-mac.dmg` |
            | Windows | `ilchenkodev-zapret-for-win.exe` |

            ### Установка
            **macOS**: Открой `.dmg`, перетащи `.app` в Applications.
            Приложение работает **только в строке меню** — в Dock его не будет.

            **Windows**: Запусти `.exe` от имени **администратора**.
            Приложение появится **только в системном трее** — без окна и без консоли.
          files: |
            artifacts/ilchenkodev-zapret-for-mac.dmg
            artifacts/ilchenkodev-zapret-for-win.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
